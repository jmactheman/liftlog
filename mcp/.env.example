# Copy to .env and fill in. NEVER commit .env (it holds a full-access secret).
LIFTLOG_SUPABASE_URL=https://ztklvpjydltpytqgleqo.supabase.co
# Supabase → Settings → API → "service_role" secret (NOT the publishable/anon key).
LIFTLOG_SUPABASE_SERVICE_KEY=your-service-role-key-here
# Optional: restrict to one user's rows (your auth user id). Leave unset for single-user.
# LIFTLOG_USER_ID=
